name: os_intents_cli
description: "Carries generated os_intents code into the native project, and verifies it arrived."
version: 0.0.1
repository: https://github.com/nugmanov/os_intents
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  args: ^2.6.0
  crypto: ^3.0.6
  os_intents_gen: ^0.0.1
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

executables:
  os_intents:
