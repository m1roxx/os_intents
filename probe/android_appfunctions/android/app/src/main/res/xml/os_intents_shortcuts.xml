<?xml version="1.0" encoding="utf-8"?>
<!--
  GENERATED BY os_intents — do not edit.

  Regenerate with:
    dart run build_runner build && dart run os_intents_cli:os_intents sync
-->
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">

  <capability android:name="actions.intent.CREATE_TASK">
    <intent
        android:action="dev.osintents.action.RUN"
        android:targetPackage="dev.osintents.appfunctions_probe"
        android:targetClass="dev.osintents.appfunctions_probe.MainActivity"
        android:data="osintents://intent/addTask">
      <parameter
          android:name="task.name"
          android:key="title" />
    </intent>
  </capability>

  <shortcut
      android:shortcutId="dueToday"
      android:enabled="true"
      android:shortcutShortLabel="@string/os_intents_dueToday_label_short"
      android:shortcutLongLabel="@string/os_intents_dueToday_label_long">
    <intent
        android:action="dev.osintents.action.RUN"
        android:targetPackage="dev.osintents.appfunctions_probe"
        android:targetClass="dev.osintents.appfunctions_probe.MainActivity"
        android:data="osintents://intent/dueToday" />
  </shortcut>

  <shortcut
      android:shortcutId="openInbox"
      android:enabled="true"
      android:shortcutShortLabel="@string/os_intents_openInbox_label_short"
      android:shortcutLongLabel="@string/os_intents_openInbox_label_long">
    <intent
        android:action="dev.osintents.action.RUN"
        android:targetPackage="dev.osintents.appfunctions_probe"
        android:targetClass="dev.osintents.appfunctions_probe.MainActivity"
        android:data="osintents://intent/openInbox" />
  </shortcut>

</shortcuts>
